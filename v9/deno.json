{
  "tasks": {
    "format": "deno fmt",
    "lint": "deno lint",
    "test": "deno test -A",
    "start": "deno run -A server.ts"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
