{
  "compilerOptions": {
    "lib": ["esnext", "dom"],
    "strict": true
  },
  "tasks": {
    "build": "deno run --allow-read --allow-write build.ts",
    "dev": "deno task build && deno run --allow-net --allow-read src/server.ts",
    "test": "deno test --allow-read"
  }
}
